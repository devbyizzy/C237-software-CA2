<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>RPConnect - Forum</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

  <header class="navbar">
    <div class="navbar-left">
      <button class="hamburger" id="hamburgerBtn" aria-label="Toggle menu">☰</button>
      <a href="/" class="logo">🎓 RPConnect</a>
    </div>
    <form class="navbar-search" action="#" method="GET" onsubmit="return false;">
      <span class="search-icon">🔍</span>
      <input type="text" name="q" placeholder="Search students, questions, groups..." />
    </form>
    <div class="navbar-right">
      <button class="icon-btn notif-btn" aria-label="Notifications" title="Notifications">🔔</button>
      <div class="navbar-profile dropdown">
        <button class="navbar-profile-trigger" id="profileMenuBtn">
          <div class="avatar avatar-sm" id="navAvatar"></div>
          <span class="navbar-username" id="navUsername"></span>
          <span class="caret">▾</span>
        </button>
        <div class="dropdown-menu" id="profileMenu">
          <a href="/profile" class="dropdown-item">🙍 My Profile</a>
          <a href="#" class="dropdown-item logout-item">🚪 Logout</a>
        </div>
      </div>
    </div>
  </header>

  <div class="layout">
    <aside class="sidebar" id="sidebar">
      <nav class="sidebar-nav">
        <a href="/" class="sidebar-link"><span class="sidebar-icon">🏠</span> Home</a>
        <a href="/groups" class="sidebar-link"><span class="sidebar-icon">👥</span> Groups</a>
        <a href="/interest-groups" class="sidebar-link"><span class="sidebar-icon">✨</span> Interest Groups</a>
        <a href="/forum" class="sidebar-link active"><span class="sidebar-icon">💬</span> Forum</a>
        <a href="/profile" class="sidebar-link"><span class="sidebar-icon">🙍</span> My Profile</a>
      </nav>
    </aside>

    <main class="feed">
      <h1 class="feed-title">💬 Forum</h1>
      <p style="font-size:14px; color:var(--text-muted); margin-top:-4px;">
        Ask a question or start a discussion for the whole school to see.
      </p>

      <section class="feed-section">
        <div class="card">
          <h2 style="margin-top:0;">Create a Post</h2>
          <div id="formError" class="form-error" style="display:none;"></div>

          <form id="questionForm">
            <div class="form-group">
              <label class="form-label" for="title">Title <span style="color:#e11d48;">*</span></label>
              <input class="form-input" type="text" id="title" name="title" maxlength="200" required placeholder="e.g. Is C237 hard with no coding background?" />
            </div>

            <div class="form-group">
              <label class="form-label" for="content">Details <span style="color:#e11d48;">*</span></label>
              <textarea class="form-textarea" id="content" name="content" rows="3" required placeholder="Give a bit more context..."></textarea>
            </div>

            <div class="form-group">
              <label class="form-label" for="category">Category</label>
              <input class="form-input" type="text" id="category" name="category" maxlength="100" placeholder="e.g. Modules, Food, Events" />
            </div>

            <div class="form-actions">
              <button type="submit" class="btn btn-primary">Post</button>
            </div>
          </form>
        </div>
      </section>

      <section class="feed-section">
        <div class="card">
          <form id="filterForm" class="search-filters">
            <div class="form-group" style="margin-bottom:0; grid-column:1 / -1;">
              <label class="form-label" for="searchInput">Search</label>
              <input class="form-input" type="text" id="searchInput" placeholder="Search posts..." />
            </div>
            <div class="form-group" style="margin-bottom:0;">
              <label class="form-label" for="categoryInput">Category</label>
              <input class="form-input" type="text" id="categoryInput" placeholder="Filter by category" />
            </div>
            <div class="form-group" style="margin-bottom:0;">
              <label class="form-label" for="sortSelect">Sort By</label>
              <select class="form-input" id="sortSelect">
                <option value="newest">Newest first</option>
                <option value="oldest">Oldest first</option>
                <option value="title">Title (A–Z)</option>
              </select>
            </div>
            <div class="form-actions" style="margin-top:0;">
              <button type="submit" class="btn btn-primary">Search</button>
            </div>
          </form>
        </div>
      </section>

      <section class="feed-section">
        <p class="results-count" id="resultsCount"></p>
        <div class="search-results-grid" id="questionResults"></div>
      </section>
    </main>
  </div>

  <script>
    window.CURRENT_USER = <%- JSON.stringify(user || null).replace(/</g, '\\u003c') %>;
  </script>
  <script src="/js/config.js"></script>
  <script src="/js/nav.js"></script>
  <script src="/js/forum.js"></script>
</body>
</html>
