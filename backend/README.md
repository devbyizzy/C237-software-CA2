# RPConnect Backend

Express REST API serving hardcoded sample data (no database).

## Run

```
npm install
npm start        # http://localhost:3000
# or
npm run dev       # nodemon, auto-restart
```

## Endpoints

- `GET /api/dashboard` - dashboard feed data (questions, groups, CCAs, timetables, trending, suggested students, current user/profile)
