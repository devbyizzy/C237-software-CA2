# TODO

- [ ] Update `frontend/css/style.css` with a unique Neon Purple + Gold theme (brand palette via CSS variables).
- [ ] Add subtle background gradient/glow/grid accents to make it more eye-catching.
- [ ] Update hover/active states, badges, buttons, avatar, dropdown, and mini cards to match the new palette.
- [ ] Remove hardcoded red inline border in `frontend/js/main.js` and replace with theme-consistent color.

- [x] Run/verify frontend in browser to ensure the new colors render correctly.



